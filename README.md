# React App

## Command

```bash
# start the frontend in react
cd frontend
npm start
```

# Backend API
## Command

```bash
# start the backend api in react
cd backend/routes
uvicorn main:app --reload --port 5000
```